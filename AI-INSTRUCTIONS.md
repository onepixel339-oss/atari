# 🤖 دليل تعديل أسئلة «سلطان الأتاري» — تعليمات إلزامية لأي مساعد AI

> **لصاحب اللعبة:** انسخ الملف ده كله + ملف `questions.js` وابعتعهم لأي مساعد ذكاء اصطناعي (ChatGPT / Claude / Gemini / أي واحد) مع طلبك. الدليل ده هيخليه يشتغل صح من أول مرة من غير ما يبوظ اللعبة.
>
> **رسالة جاهزة تنسخها مع الملفين:**
> ```
> انت هتعدل ملف أسئلة لعبة. الدليل المرفق (AI-INSTRUCTIONS.md) إلزامي والتزم بيه حرفياً.
> المرفقات: الدليل + ملف questions.js الحالي.
> طلبي: [اكتب هنا — مثال: استبدل أسئلة الثقافة العامة بعشرة أسئلة جديدة عن تاريخ مصر]
> في الآخر سلمني ملف questions.js كاملاً جاهز للنسخ واللصق.
> ```

---

## 1) أنت شغال على إيه؟ (معلومات المشروع — اقرأها كويس)

**«سلطان الأتاري»** لعبة أسئلة ويب أركيد بكسل-ريترو نيون بالعامية المصرية. ملفات HTML/CSS/JS ثابتة — **أنت هتعدل ملف واحد بس: `data/questions.js`** ومش هتلمس أي ملف تاني.

**رحلة اللاعب في اللعبة:**
1. شاشة دخول (اسم + رمز) — وفوقها **شاشة جذب «★ HIGH SCORE ★»** بستايل ATTRACT MODE بتعرض **الريكورد المحلي للجهاز** (أحسن نتيجة اتحطت عليه + صاحبها — أو نداء «كون انت الأول» لو مفيش ريكورد)
2. الصالة فيها **3 مراحل (كروت ألعاب)**: ثقافة عامة / حساب وذكاء / ألغاز وتفكير
3. كل مرحلة = مجموعة أسئلة بتتلعب واحد ورا التاني بمؤقت لكل سؤال — وكل **3 إجابات صح ورا بعض** بتنط لفحة «بوم!» متدرجة: 3 صح = «بوم!» · 6 = «بوم! بوم!» · 9 = «سلطان!» (والشاشة بتهتز) · 12+ = «سلطان بلا منافس!»
4. بعد الـ 3 مراحل: **جولة المخاطرة (تحدي العباقرة)** — أسئلة صعبة، صح = مكافأة كبيرة وغلط = خصم
5. الكأس — **بتتشارك في جروبات الأصحاب ودي المنافسة الحقيقية** (زرار مشاركة/تحميل جاهز). على الجهاز المحلي بيتسجل **ريكورد واحد بس** (`atariRecordScore`)

**ليه حقل `time` مهم جداً؟** النقاط بتتحسب على **تلت الوقت بتاع كل سؤال نفسه**: جاوب صح في التلت الأول = **15 نقطة**، التاني = **10**، الأخير = **5**، غلط = **0**. عشان كده كل مستوى ليه زمن ثابت مينفعش تتلعب بيه.

---

## 2) ملف `questions.js` — مكوناته الستة (كلها لازم تفضل موجودة)

```js
const MAIN_QUESTIONS  = [ ... ];  // الأسئلة العادية (الثقافة/الحساب/الألغاز)
const GENIUS_QUESTIONS = [ ... ]; // جولة المخاطرة (تحدي العباقرة)
const CATEGORY_META    = { ... }; // بيانات الفئات الثلاثة (اسم معروض + كلاس لون)
const DESK_CATEGORIES  = [ ... ]; // ['culture','sport','logic'] ترتيب مراحل الصالة
const EVAL_PHRASES     = { ... }; // عبارات التقييم (5 مستويات × 4 عبارات)
const MARGIN_NOTES     = [ ... ]; // الحواشي الهامشية اللي بتظهر جنب بعض الأسئلة
```

### ⚠️ قاعدة البصمة (مهمة أوي — قولها لصاحب اللعبة)
اللعبة بتحسب بصمة محتوى من `MAIN_QUESTIONS` + `GENIUS_QUESTIONS` بس. يعني:
- **أي تعديل ولو حرف واحد** في السؤالين دول → أول مرة حد يفتح اللعبة: الريكورد المحلي والحفظ القديم **يتمسح أوتوماتيك** ويظهر إشعار «🔔 جولة جديدة بدأت». **ده مقصود وده المطلوب** — النقاط القديمة ما تختلطش بالجديدة.
- تعديل `EVAL_PHRASES` أو `MARGIN_NOTES` أو `CATEGORY_META` **مش** بيبصم (مفيش تصفير).
- نبه صاحب اللعبة في ردك: «تعديلك ده هيبدأ جولة جديدة وهتصفّر الريكورد المحلي».

### 🔒 قاعدة الدخلة الواحدة + الذاكرة الاحتياطية (Task 34)
- كل لاعب ليه **دخلة واحدة في الجولة** — بتتسجل أول ما الكأس تظهر، وبعد كده شاشة الدخول بتقفل على كارت «إنت داخل خلاص يا بطل».
- التسجيل متخزن في **4 مخازن**: `localStorage` (`diwanRegisteredName`) + كوكي (`atariMem`) + IndexedDB (`atariGuardDB`) + Cache API (`atari-guard-v1`) — وكل نسخة فيها بصمة الموسم اللي اتسجلت فيها.
- لو اللاعب مسح بيانات الموقع من المتصفح، النسخ الاحتياطية بترجّع القفل لوحدها **ما دام نفس الموسم** — وعشان كده تصفير الموسم (تغيير البصمة) بيمسح النسخ الاحتياطية كمان، فالجولة الجديدة بتكون فرصة جديدة للكل فعلاً.
- لو المتصفح في وضع التخفي أو بيقفل التخزين، بتظهر تنبيه «👁️ وضع بلا ذاكرة» — اللعبة بتشتغل عادي بس الدخلة مش بتتحفظ.
- حدود الصراحة: «Clear site data» الكامل بيمسح كل المخازن مرة واحدة — الحماية دي ضد العبث مش ضد القرصنة، وأي حماية أقوى محتاجة backend (مش متاح في النسخة الثابتة).

---

## 3) صيغة السؤال الواحد (التزم حرفياً)

```js
{ cat:'culture', catLabel:'ثقافة عامة', levelLabel:'⚡ سريع', time:15,
  text:'نص السؤال؟',
  options:['الاختيار الأول','التاني','التالت','الرابع'],
  correct:2,
  hint:'تلميح ذكي مش بيكشف الإجابة',
  explanation:'شرح تفصيلي للإجابة الصح بيظهر في المراجعة' },
```

### الحقول الإجبارية (8):
| الحقل | النوع | القواعد |
|---|---|---|
| `cat` | نص | واحد من **4 بس**: `'culture'` / `'sport'` / `'logic'` / `'genius'` |
| `catLabel` | نص | `culture→'ثقافة عامة'` · `sport→'حساب وذكاء'` · `logic→'ألغاز وتفكير'` · `genius→'تحدي العباقرة'` |
| `levelLabel` | نص | حسب المستوى (الجدول تحت) — **بالإيموجي بتاعه بالظبط** |
| `time` | رقم | حسب المستوى (الجدول تحت) — **مش نص، رقم صريح** |
| `text` | نص | سطر واحد، من غير علامات `''` جواه إلا مهرّبة |
| `options` | مصفوفة | **4 نصوص بالظبط**، ممنوع التكرار بينهم |
| `correct` | رقم | **0 أو 1 أو 2 أو 3** — موضع الإجابة الصح (مش نص!) |
| `hint` | نص | تلميح يمشّي اللاعب من غير ما يقول الإجابة |
| `explanation` | نص | شرح كامل مفيد — بيظهر آخر كل مرحلة في المراجعة |

### حقول اختيارية (رايات — في مكانها بالظبط بس):
| الراية | مين بيحطها | بتعمل إيه |
|---|---|---|
| `allowAppeal:true` | أسئلة ⏱️ متوسط **بس** | بيظهر زرار الوقت الزايد (+10 ثواني) |
| `witness2:true` | أسئلة 🧠 ذكي **بس** | بيفعّل «لحظة الحقيقة» (تثبيت الإجابة = ×1.5 نقاط السرعة) |
| ممنوع | أسئلة 🎯 عبقري | من غير أي راية خالص (الجولة فيها نظام نقاطها الخاص) |

### المستويات الأربعة (لا تخترع مستويات جديدة):
| المستوى | `levelLabel` بالحرف | `time` المسموح | رايات |
|---|---|---|---|
| ⚡ سريع | `'⚡ سريع'` | `15` أو `20` | ولا حاجة |
| ⏱️ متوسط | `'⏱️ متوسط'` | `30` | `allowAppeal:true` **إجبارية** |
| 🧠 ذكي | `'🧠 ذكي'` | `90` أو `120` | `witness2:true` **إجبارية** |
| 🎯 عبقري | `'🎯 عبقري'` | `90` | ممنوع أي راية |

### التوزيع المعتمد دلوقتي (حافظ عليه إلا لو صاحب اللعبة طلب غير كده):
- **culture**: 10 أسئلة — كلها ⚡ سريع
- **sport**: 10 أسئلة — 3 ⚡ سريع + 3 ⏱️ متوسط + 4 🧠 ذكي
- **logic**: 10 أسئلة — 3 ⚡ سريع + 4 ⏱️ متوسط + 3 🧠 ذكي
- **genius**: 7 أسئلة 🎯 عبقري
- المجموع الحالي: 37 سؤال — وأقصى مجموع ممكن = (عدد العاديين × 15) + (عدد العباقرة × 20) = **590**
- اللعبة بتحسب أقصى المجموع ديناميكياً فمش هتقع لو العدد اتغير — بس **التوازن المصمم** هو اللي فوق، ومتغيرش الأعداد إلا بطلب صريح من صاحب اللعبة.
- ⛔ **فئة من غير أسئلة = مرحلتها بتختفي من الصالة** — ممنوع أي فئة من الثلاثة تبقى فاضية.
- عدد العباقرة معروض في واجهة الجولة كـ«7 أسئلة» — لو هتغيره قول لصاحب اللعبة إن محتاج تعديل سطر في الواجهة.

---

## 4) القواعد القاتلة (أي واحدة فيهم = اللعبة تقع أو تتلخبط)

1. **`cat` من بره الأربعة المسموحين = انهيار**: اللعبة بتقرأ `CATEGORY_META[cat]` وأي قيمة جديدة مش موجودة فيه → خطأ جافاسكريبت واللعبة مش هتفتح. ممنوع تماماً تخترع `cat` جديد.
2. **ممنوع تمسح أو تغير اسم أي ثابت** من الستة (`MAIN_QUESTIONS`, `GENIUS_QUESTIONS`, `CATEGORY_META`, `DESK_CATEGORIES`, `EVAL_PHRASES`, `MARGIN_NOTES`) — لو مسحته اللعبة مش هتفتح خالص.
3. **ممنوع تضيف `window.MAIN_QUESTIONS = ...` أو أي export** — الملف بيتحمل كسكريبت عادي قبله ملفات تانية بتقرأ منه بالاسم مباشرة.
4. **ممنوع تغيير `time` بره الجدول** — ثلث الوقت هو أساس النقاط، وزمن غلط = نقاط غلط.
5. **الاقتباسات**: كل النصوص جوه علامة `'` مفردة. لو النص محتاج `'` جواه (زي: «العالم 'بيتأكد'») لازم تهربها `\'` — **الأحسن: استبدلها بعلامة « » أو اعادة صياغة**. لو النص فيه `"..."` مزدوجة ده مسموح عادي.
6. **ممنوع سطر جديد جوه النص** — كل نص في سطر واحد. النص الطويل يفضل في سطره.
7. **`correct` لازم يكون رقم** — `correct:'2'` غلط، `correct:4` غلط، `correct:-1` غلط.
8. **الإجابة الصح واحدة فعلاً** — ممنوع اختيارين يفتحوا لبعض (مثال: «القاهرة» و«عاصمة مصر» في نفس السؤال).
9. **ممنوع سؤال متكرر** — ولا نصاً ولا معنى (سؤالين بصياغتين مختلفين لنفس المعلومة = تكرار).
10. **الفواصل**: بين كل حقل وآخر `,` وبين كل سؤال وآخر `,` — راجع آخر سؤال في كل مصفوفة (متسيبش فاصلة معلقة تكسر الترميز، واتبع ستايل الملف الأصلي بالظبط).
11. **الترميز UTF-8 عربي سليم** — ممنوع رموز مش قابلة للقراءة أو نصوص مقلوبة.

---

## 5) جودة المحتوى (عشان الأسئلة تبقى مستواها كويس)

- **اللهجة**: السؤال والإجابات بالفصحى الواضحة البسيطة، والتلميح والشرح **بلمسة مصرية خفيفة** (نفس روح الملف الحالي — اقرأ 5 أسئلة منه قبل ما تكتب).
- **الأطوال**: `text` لحد ~140 حرف (يتقري على الموبايل في ثواني)، `options` قصيرة ومتسقة في النمط (لو فيها وحدات خليها في كل الاختيارات: «25 جنيهًا» / «30 جنيهًا»...).
- **موضع الإجابة الصح**: نوّع — مفتعش كل الإجابات الصح في نفس الرقم. وزّع بين 0 و1 و2 و3.
- **الدقة الرياضية إجبارية**: في أي سؤال حساب أو منطق، **حل السؤال خطوة بخطوة بنفسك قبل ما تكتبه** وتتأكد إن الإجابة الصح فعلاً هي الموجودة في `options[correct]` — وإن باقي الاختيارات **نتايج غلط منطقية** (نتيجة خطأ شائع، مش أرقام عشوائية).
- **التلميح**: يقرب اللاعب للطريق (استبعاد، نمط، خطوة أولى) من غير ما يقول الإجابة.
- **الشرح**: 1–3 جمل — بيقول الصح وسبب غلط أشهر بديل لو مفيد.
- **محتوى آمن**: مناسب لكل الأعمار، بعيد عن السياسة والأديان الطائفية والمحتوى الحساس. أسئلة معلومات عامة / حساب / ألغاز / ترفيه ثقافي.
- **صعوبة العباقرة**: أسئلة مركبة (خطوتين حساب أو لغز خداع) — مش بس «صعب»، لازم تكون **ممتعة** في الحل والخداع زي الموجودين.

---

## 6) خطوات الشغل الإلزامية (بالترتيب)

1. **اقرأ ملف `questions.js` المرفق كله أول** — افهم الستايل والتوزيع والمستويات.
2. **فهم الطلب**: لو صاحب اللعبة مش محدد (استبدال؟ إضافة؟ فئة معينة؟ موضوع؟) — **اسأل سؤال واحد واضح** قبل ما تكتب.
3. **اكتب الأسئلة الجديدة** بنفس صيغة وستايل الملف حرفياً.
4. **راجع نفسك**: طبّق قائمة الفحص (قسم 7) وامشي على كل سؤال واحد واحد.
5. **حدّث تعليق الهيدر** أول الملف لو عدد الأسئلة اتغير (السطر اللي بيقول «بنك الأسئلة: 37 سؤال...»).
6. **سلّم الملف كله**: محتوى `questions.js` **كامل من أول سطر لآخر سطر** جوه code block واحد بلغة `js` — حتى لو عدلت سؤالين بس. صاحب اللعبة هينسخه كله ويحطه مكان الملف القديم. **ممنوع تبعت مقاطع أو «غيّر السطر ده بس»** — صاحب اللعبة مش مبرمج.
7. **في ردك** قول كمان: كام سؤال اتغير/اتضاف، وإن التعديل هيبدأ «جولة جديدة» (لو لامس مصفوفات الأسئلة).

---

## 7) قائمة الفحص النهائية — اعملها على **كل سؤال** قبل التسليم

- [ ] `cat` من الأربعة المسموحين و`catLabel` مطابق ليه
- [ ] `levelLabel` بالإيموجي بالحرف من الجدول
- [ ] `time` رقم مطابق للمستوى (15/20 · 30 · 90/120 · 90)
- [ ] `allowAppeal:true` على كل متوسط — `witness2:true` على كل ذكي — ولا حاجة على سريع وعبقري
- [ ] 4 اختيارات مختلفة + `correct` رقم 0–3 يشاور فعلاً على الصح
- [ ] حلّت أي سؤال حساب/منطق بنفسي والنتيجة مطابقة
- [ ] الإجابة الصح موضعها متنوع مش ثابت
- [ ] مفيش سؤال متكرر ولا اختيارين صح
- [ ] مفيش `'` مش هرب جوه نص، ولا سطر جديد جوه نص
- [ ] التوزيع الإجمالي زي القسم 3 (غير لو طُلب غير كده) ومفيش فئة فاضية
- [ ] تعليق الهيدر اتحدث لو العدد اتغير
- [ ] سلّمت **الملف كاملاً** في code block واحد

---

## 8) نظام النقاط (خلفية سريعة — عشان تفهم ليه القواعد دي)

| الحالة | النقاط |
|---|---|
| صح في تلت وقت السؤال الأول | +15 |
| صح في التلت التاني | +10 |
| صح في التلت الأخير | +5 |
| غلط في السؤال العادي | 0 (مفيش خصم) |
| جولة المخاطرة: صح | +20 |
| جولة المخاطرة: غلط | −12 |
| اللمبة (وسيلة مساعدة) | −15 |
| الوقت الزايد | +10 ثواني والثلث بيتحسب على الوقت الممتد |
| لحظة الحقيقة (أسئلة 🧠) | تثبيت الإجابة = ×1.5 نقاط السرعة |

أقصى مجموع ديناميكي = (عدد العاديين × 15) + (عدد العباقرة × 20) — حالياً 590.

---

## 9) فاحص فوري — لصاحب اللعبة (من غير أي برمجة)

بعد ما تحط الملف الجديد مكان القديم وافتح اللعبة: اضغط **F12** (أو زر المراجعة في الموبايل → Console)، انسخ الكود ده كله والزقه في الـ Console واضغط Enter:

```js
(function فحص_الملف(){
  const p=[]; const push=m=>p.push(m);
  const cats=['culture','sport','logic','genius'];
  const levels={'⚡ سريع':[15,20],'⏱️ متوسط':[30],'🧠 ذكي':[90,120],'🎯 عبقري':[90]};
  function فحص(q,i,isB){
    ['cat','catLabel','levelLabel','time','text','options','correct','hint','explanation']
      .forEach(f=>{ if(q[f]===undefined) push('س'+(i+1)+': ناقص الحقل '+f); });
    if(!cats.includes(q.cat)) push('س'+(i+1)+': cat="'+q.cat+'" مش من الأربعة المسموحين');
    if(q.cat!=='genius' && !CATEGORY_META[q.cat]) push('س'+(i+1)+': فئة مش موجودة في CATEGORY_META');
    if(!isB && q.cat==='genius') push('س'+(i+1)+': سؤال عبقري جوه MAIN_QUESTIONS (مكانه GENIUS_QUESTIONS)');
    if(!isB && q.levelLabel==='🎯 عبقري') push('س'+(i+1)+': مستوى "🎯 عبقري" ممنوع جوه الأسئلة العادية');
    const ok=levels[q.levelLabel];
    if(!ok) push('س'+(i+1)+': levelLabel="'+q.levelLabel+'" مش معروف');
    else if(!ok.includes(q.time)) push('س'+(i+1)+': time='+q.time+' مش مناسب لمستوى "'+q.levelLabel+'"');
    if(!Array.isArray(q.options)||q.options.length!==4) push('س'+(i+1)+': الاختيارات لازم 4');
    else if(new Set(q.options).size!==4) push('س'+(i+1)+': فيه اختيارات مكررة');
    if(typeof q.correct!=='number'||q.correct<0||q.correct>3) push('س'+(i+1)+': correct لازم رقم من 0 لـ 3');
    if(isB && (q.witness2||q.allowAppeal)) push('س'+(i+1)+': أسئلة العباقرة من غير رايات');
    if(!isB && q.levelLabel==='⏱️ متوسط' && !q.allowAppeal) push('س'+(i+1)+': متوسط ناقصه allowAppeal:true');
    if(!isB && q.levelLabel==='🧠 ذكي' && !q.witness2) push('س'+(i+1)+': ذكي ناقصه witness2:true');
  }
  const mc={}; MAIN_QUESTIONS.forEach((q,i)=>{فحص(q,i,false); mc[q.cat]=(mc[q.cat]||0)+1;});
  GENIUS_QUESTIONS.forEach((q,i)=>فحص(q,i,true));
  ['culture','sport','logic'].forEach(c=>{ if(!mc[c]) push('فئة '+c+' بقت فاضية — المرحلة هتتشال من الصالة!'); });
  const seen=new Set();
  MAIN_QUESTIONS.concat(GENIUS_QUESTIONS).forEach((q,i)=>{
    const k=(q.text||'').trim(); if(seen.has(k)) push('س'+(i+1)+': نص السؤال متكرر'); seen.add(k);
  });
  const cnt=JSON.stringify({ثقافة:mc.culture||0, حساب:mc.sport||0, ألغاز:mc.logic||0, عباقرة:GENIUS_QUESTIONS.length});
  console.log('التوزيع:', cnt, '· أقصى مجموع:', MAIN_QUESTIONS.length*15+GENIUS_QUESTIONS.length*20);
  console.log(p.length? '❌ فيه '+p.length+' مشكلة:\n'+p.join('\n') : '✅ الملف سليم 100% — جاهز');
})();
```

- لو طلع **✅ الملف سليم 100%** — تمام، محتاج تعمل حاجة تانية.
- لو طلعت **❌ مشاكل** — انسخ الرسالة وابعتها للـ AI وقاله «صلّح دي وسلمني الملف كامل تاني».

> ملحوظة: الفاحص بيكشف الأخطاء الشكلية (الحقول والأرقام والتكرار). **دقة الإجابات الصح نفسها** مسؤولية الـ AI — عشان كده الدليل واضح إنه لازم يحل أي سؤال حساب بنفسه قبل التسليم.
